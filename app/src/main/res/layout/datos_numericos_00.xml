<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/White"
    android:orientation="vertical">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:id="@+id/datos_numericos_content">

        <android.support.design.widget.FloatingActionButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            app:srcCompat="@drawable/ic_play_arrow_black_24dp"
            android:tint="@color/White"
            android:id="@+id/datos_numericos_fab"
            android:layout_gravity="bottom|end"/>
    </FrameLayout>

    <android.support.design.widget.BottomNavigationView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/datos_numericos_bottom_navigation"
        app:menu="@menu/menu_datos_numericos"
        android:background="?colorAccent"
        app:itemTextColor="@drawable/menu_selector_datos_numericos"
        app:itemIconTint="@drawable/menu_selector_datos_numericos"
        app:itemBackground="?colorAccent">

    </android.support.design.widget.BottomNavigationView>

</LinearLayout>